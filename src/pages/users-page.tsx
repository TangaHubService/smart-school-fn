import { StateView } from '../components/state-view';

export function UsersPage() {
  return (
    <StateView
      title="Users module placeholder"
      message="Role-aware navigation and route protection are active. User management UI lands in the next sprint."
    />
  );
}
